# Normalize text files to LF in the repo
* text=auto eol=lf

# Explicit for common source files
*.{js,jsx,ts,tsx,css,scss,html,json,md,yml,yaml} text eol=lf

# Keep Windows endings for Windows scripts
*.{bat,cmd}        text eol=crlf
*.ps1              text eol=crlf
